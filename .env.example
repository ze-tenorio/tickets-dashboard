# Jira (obrigatório para sync)
JIRA_BASE_URL=https://sua-instancia.atlassian.net
JIRA_EMAIL=seu-email@empresa.com
JIRA_API_TOKEN=seu-api-token-atlassian
# JQL opcional; padrão: order by created DESC
# JIRA_JQL=project = ID ORDER BY created DESC

# Google Sheets (obrigatório para sync)
# ID da planilha: na URL https://docs.google.com/spreadsheets/d/ESTE_E_O_ID/edit
GOOGLE_SHEET_ID=
# Nome da aba onde os tickets serão escritos (padrão: Tickets)
# GOOGLE_SHEET_TAB=Tickets

# Credenciais Google (uma das duas)
# Opção 1: caminho para o arquivo JSON da service account
# GOOGLE_APPLICATION_CREDENTIALS=./secrets/google-service-account.json
# Opção 2: conteúdo do JSON (útil em CI) - use GOOGLE_SERVICE_ACCOUNT_JSON
